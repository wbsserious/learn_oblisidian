# GPT
Generative Pre-trained Transformer
it stacks decoder blocks similar to the encoder-stacked architecture of BERT.
它把**解码器块**层层堆叠，形成与 BERT 的编码器堆叠架构类似的结构。

![[Pasted image 20251224111557.png]]
Generative LLMs, as sequence-to-sequence machines, take in some text and attempt to autocomplete it.
生成式大语言模型（Generative LLMs）作为“序列到序列”机器，会接收一段文本并尝试为其**自动补全**后续内容。
Although a handy feature, their true power shone from being trained as a chatbot.
虽然是一项便利的功能，但它们真正的威力是在被训练成**聊天机器人**时才显现出来。
