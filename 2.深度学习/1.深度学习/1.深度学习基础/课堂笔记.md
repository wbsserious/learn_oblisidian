你想了解张量（通常指 PyTorch/TensorFlow 中的 Tensor）和 NumPy 数组（ndarray）的区别，核心结论是：**两者在数据结构和数值计算上高度相似（张量可以看作「增强版」NumPy 数组），但核心差异在于「是否支持自动求导」和「是否适配硬件加速（GPU/TPU）」—— 这也是张量专为深度学习设计的关键特性**。
需要手打一遍各种操作内容
连续张量和不连续张量的区别和存储区别
cat 和 stack的区别：
cat连接**不会**改变原始数组的维度，按照cat中的dim指标针对相应的维度进行拼接除了dim指定的维度形状不一致，剩下的维度都需要一致
stack连接会改变原始数组的维度，
