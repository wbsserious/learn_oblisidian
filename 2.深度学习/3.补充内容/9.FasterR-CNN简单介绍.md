# Faster R-CNN 是什么？
它是 **做目标检测（物体定位 + 分类）** 的经典模型，用来：
- 找出图片里的人、车、狗、桌子…
- 给出每个物体的 **框坐标（x1,y1,x2,y2）**
- 同时给出类别（人 / 车 / 猫…）
它的流程：
1. 生成候选框（Anchor）
2. 修正框的位置（**回归任务**）
3. 分类框里是什么物体（**分类任务**）
**Faster R-CNN 是经典目标检测模型，它的边界框回归必须用 Smooth L1 Loss，因为它既稳定又不怕异常值，是检测任务的标准设计。**